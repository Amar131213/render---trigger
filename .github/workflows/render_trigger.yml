name: Trigger Render Daily

on:
  schedule:
    - cron: '30 11 * * 1-5'  # हर सोमवार से शुक्रवार, शाम 5:00 बजे IST
  workflow_dispatch:

jobs:
  trigger_render:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Webhook
        run: |
          echo "🔔 Triggering Render Webhook..."
          curl -X GET "https://screener-scraper.onrender.com/run"

      - name: Wait for 5 minutes
        run: |
          echo "⏳ Waiting for 5 minutes to let scraper start..."
          sleep 300  # 300 seconds = 5 minutes
          echo "✅ Wait complete. Assuming scraper has started."
