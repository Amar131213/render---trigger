name: Trigger Render Scraper

on:
  schedule:
    - cron: '30 11 * * 1-5'  # ⏰ सोमवार से शुक्रवार, शाम 5:00 IST (11:30 UTC)
  workflow_dispatch:         # 🔘 Manual run का भी option

jobs:
  call_scraper:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger /run endpoint on Render
        run: |
          curl -X GET "https://screener-scraper.onrender.com/run"
